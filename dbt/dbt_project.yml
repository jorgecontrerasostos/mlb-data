name: mlb_data_dbt

profile: mlb_data_dbt

seed-paths: ["seeds"]
model-paths: ["models"]
macro-paths: ["macros"]
clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  # Builds seeds into '<your_schema_name>_raw'
  mlb_data_dbt:
    +schema: raw

models:
  mlb_data_dbt:
    # Materialize staging models as views, and marts as tables
    silver:
      +materialized: view
    gold:
      +materialized: table
